---
const { white, multiplier = 0.9, class: classList } = Astro.props
const size = 756
---

<div
  id="circles"
  class:list={[
    'pointer-events-none rounded-full',
    white ? 'bg-paper' : 'bg-coral',
    classList,
  ]}
  style={{
    width: `${multiplier * size}px`,
    height: `${multiplier * size}px`,
  }}
/>
